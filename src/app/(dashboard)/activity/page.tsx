import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ActivityPage() {
  return <PlaceholderPage title="Activity" description="A log of what changed across tenants — plan changes, suspensions, sync events." />;
}
